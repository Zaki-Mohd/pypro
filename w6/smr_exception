def validate_marks(marks):
    if marks < 0 or marks > 100:
        raise Exception("Invalid marks! Marks should be between 0 and 100.")
    return "Marks are valid."

try:
    print(validate_marks(25))
except Exception as e:
    print(str(e))
